{
	"name": "@nexus/config",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"typecheck": "echo 'no typcheck for config'",
		"lint": "biome lint .",
		"build": "echo 'no build for config'"
	}
}
