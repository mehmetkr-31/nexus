{
  "name": "nexus",
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "dev": "bun run --filter '*' dev"
  }
}
